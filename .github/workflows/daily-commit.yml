mkdir -p .github/workflows

cat > .github/workflows/daily-commit.yml << 'EOF'
name: Daily Commit

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Test
        run: echo "Workflow works"
EOF